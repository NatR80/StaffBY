<UserControl x:Class="StaffBY.App.Views.UserControls.VacationsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Управление отпусками" 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"
                   Foreground="#2C3E50"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="➕ Добавить отпуск" Padding="10,5" Margin="5"
                    Background="#27AE60" Foreground="White" Click="AddVacationButton_Click"/>
            <Button Content="📅 График отпусков" Padding="10,5" Margin="5"
                    Background="#3498DB" Foreground="White" Click="VacationScheduleButton_Click"/>
            <ComboBox x:Name="YearComboBox" Width="100" Margin="10,0,0,0"/>
        </StackPanel>

        <DataGrid x:Name="VacationsDataGrid" Grid.Row="2" AutoGenerateColumns="False"
                  AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Сотрудник" Binding="{Binding EmployeeFullName}" Width="150"/>
                <DataGridTextColumn Header="Вид отпуска" Binding="{Binding VacationType}" Width="120"/>
                <DataGridTextColumn Header="Дата начала" Binding="{Binding StartDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Дата окончания" Binding="{Binding EndDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Количество дней" Binding="{Binding DaysCount}" Width="100"/>
                <DataGridTextColumn Header="Основание" Binding="{Binding Basis}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>