<UserControl x:Class="StaffBY.App.Views.UserControls.VacationsAllControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель инструментов -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="AddVacationButton" Content="➕ Добавить отпуск" 
                    Padding="10,5" Background="#3498DB" Foreground="White"
                    Click="AddVacationButton_Click"/>
            <Button x:Name="VacationScheduleButton" Content="📅 График отпусков" 
                    Padding="10,5" Margin="5,0,0,0" Background="#F39C12" Foreground="White"
                    Click="VacationScheduleButton_Click"/>
            <ComboBox x:Name="YearComboBox" Width="80" Margin="20,0,0,0" VerticalAlignment="Center"/>
            <TextBlock Text="Остаток дней по всем сотрудникам:" Margin="20,0,10,0" VerticalAlignment="Center"/>
            <TextBlock x:Name="TotalRemainingText" Text="0" FontWeight="Bold" Foreground="Green" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Таблица отпусков -->
        <DataGrid x:Name="VacationsDataGrid" Grid.Row="1" AutoGenerateColumns="False" 
                  CanUserAddRows="False" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Сотрудник" Binding="{Binding EmployeeFullName}" Width="150"/>
                <DataGridTextColumn Header="Вид отпуска" Binding="{Binding VacationType}" Width="100"/>
                <DataGridTextColumn Header="Дата начала" Binding="{Binding StartDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Дата окончания" Binding="{Binding EndDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                <DataGridTextColumn Header="Дней" Binding="{Binding DaysCount}" Width="60"/>
                <DataGridTextColumn Header="Основание" Binding="{Binding Basis}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>